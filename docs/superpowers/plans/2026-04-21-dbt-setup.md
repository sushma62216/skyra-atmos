# dbt Project Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a working, connected dbt scaffold (`weather_analytics`) inside `skyra-atmos/dbt/` that compiles cleanly and sees all 5 RAW Snowflake source tables.

**Architecture:** Manual file creation (no `dbt init` boilerplate). `dbt_project.yml` maps `staging/` → STAGING schema (views) and `analytics/` → ANALYTICS schema (tables). Credentials live in `~/.dbt/profiles.yml` via `env_var()` calls, never committed. Source tables are declared in `models/staging/sources.yml` with `_LOADED_AT` for freshness checks.

**Tech Stack:** dbt-core, dbt-snowflake (both already installed), Snowflake (`WEATHER_ANALYTICS` database, `SKYRA_ATMOS` warehouse)

---

### Task 1: Create directory structure and `dbt_project.yml`

**Files:**
- Create: `skyra-atmos/dbt/weather_analytics/dbt_project.yml`
- Create: `skyra-atmos/dbt/weather_analytics/models/staging/.gitkeep`
- Create: `skyra-atmos/dbt/weather_analytics/models/analytics/.gitkeep`
- Create: `skyra-atmos/dbt/weather_analytics/tests/.gitkeep`
- Create: `skyra-atmos/dbt/weather_analytics/macros/.gitkeep`

- [ ] **Step 1: Create directory structure**

```bash
mkdir -p skyra-atmos/dbt/weather_analytics/models/staging
mkdir -p skyra-atmos/dbt/weather_analytics/models/analytics
mkdir -p skyra-atmos/dbt/weather_analytics/tests
mkdir -p skyra-atmos/dbt/weather_analytics/macros
touch skyra-atmos/dbt/weather_analytics/models/analytics/.gitkeep
touch skyra-atmos/dbt/weather_analytics/tests/.gitkeep
touch skyra-atmos/dbt/weather_analytics/macros/.gitkeep
```

- [ ] **Step 2: Write `dbt_project.yml`**

Create `skyra-atmos/dbt/weather_analytics/dbt_project.yml`:

```yaml
name: weather_analytics
version: '1.0.0'
config-version: 2

profile: weather_analytics

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_analytics:
    staging:
      +schema: STAGING
      +materialized: view
    analytics:
      +schema: ANALYTICS
      +materialized: table
```

- [ ] **Step 3: Verify the file is valid YAML**

```bash
cd skyra-atmos/dbt/weather_analytics
python3 -c "import yaml; yaml.safe_load(open('dbt_project.yml'))" && echo "VALID"
```

Expected: `VALID`

- [ ] **Step 4: Commit**

```bash
cd skyra-atmos
git add dbt/
git commit -m "feat: scaffold dbt project structure and dbt_project.yml"
```

---

### Task 2: Add `packages.yml`

**Files:**
- Create: `skyra-atmos/dbt/weather_analytics/packages.yml`

- [ ] **Step 1: Write `packages.yml`**

Create `skyra-atmos/dbt/weather_analytics/packages.yml`:

```yaml
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
```

- [ ] **Step 2: Install packages**

```bash
cd skyra-atmos/dbt/weather_analytics
dbt deps
```

Expected output: `Installing dbt-labs/dbt_utils` followed by `Installed X packages`

- [ ] **Step 3: Verify `dbt_packages/` was created (it is gitignored)**

```bash
ls skyra-atmos/dbt/weather_analytics/dbt_packages/
```

Expected: `dbt_utils` directory present

- [ ] **Step 4: Commit**

```bash
cd skyra-atmos
git add dbt/weather_analytics/packages.yml
git commit -m "feat: add dbt_utils package dependency"
```

Note: `dbt_packages/` is already covered by `.gitignore` entry `dbt/dbt_packages/`.

---

### Task 3: Configure `~/.dbt/profiles.yml`

**Files:**
- Create or update: `~/.dbt/profiles.yml` (outside repo — never committed)

- [ ] **Step 1: Ensure `~/.dbt/` directory exists**

```bash
mkdir -p ~/.dbt
```

- [ ] **Step 2: Check if `~/.dbt/profiles.yml` already exists**

```bash
cat ~/.dbt/profiles.yml 2>/dev/null || echo "FILE NOT FOUND - creating new"
```

If the file exists and has other profiles, add the `weather_analytics` block without removing existing profiles.

- [ ] **Step 3: Write the `weather_analytics` profile**

If the file does not exist, create `~/.dbt/profiles.yml` with:

```yaml
weather_analytics:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}"
      user: "{{ env_var('SNOWFLAKE_USER') }}"
      password: "{{ env_var('SNOWFLAKE_PASSWORD') }}"
      role: "{{ env_var('SNOWFLAKE_ROLE') }}"
      database: WEATHER_ANALYTICS
      warehouse: SKYRA_ATMOS
      schema: STAGING
      threads: 4
```

If the file already exists, append the `weather_analytics:` block above to it.

- [ ] **Step 4: Load your `.env` and verify env vars are present**

```bash
cd skyra-atmos
source .env 2>/dev/null || set -a && source .env && set +a
echo "Account: $SNOWFLAKE_ACCOUNT"
echo "User: $SNOWFLAKE_USER"
echo "Role: $SNOWFLAKE_ROLE"
```

Expected: All three values print (not empty, not `your_account`). If they're still placeholders, update `skyra-atmos/.env` with your real Snowflake credentials before continuing.

---

### Task 4: Declare RAW source tables in `sources.yml`

**Files:**
- Create: `skyra-atmos/dbt/weather_analytics/models/staging/sources.yml`

- [ ] **Step 1: Write `sources.yml`**

Create `skyra-atmos/dbt/weather_analytics/models/staging/sources.yml`:

```yaml
version: 2

sources:
  - name: raw
    database: WEATHER_ANALYTICS
    schema: RAW
    description: "Raw NOAA GHCN data loaded from S3 via Snowflake external stage"
    tables:
      - name: RAW_STATIONS
        description: "129,658 weather stations with coordinates, elevation, and metadata"
        loaded_at_field: _LOADED_AT
        columns:
          - name: STATION_ID
            description: "11-character station identifier"
          - name: LATITUDE
          - name: LONGITUDE
          - name: ELEVATION
          - name: STATE
          - name: STATION_NAME
          - name: GSN_FLAG
          - name: HCN_CRN_FLAG
          - name: WMO_ID

      - name: RAW_COUNTRIES
        description: "219 country codes and names"
        loaded_at_field: _LOADED_AT
        columns:
          - name: COUNTRY_CODE
          - name: COUNTRY_NAME

      - name: RAW_STATES
        description: "74 US state and province codes and names"
        loaded_at_field: _LOADED_AT
        columns:
          - name: STATE_CODE
          - name: STATE_NAME

      - name: RAW_INVENTORY
        description: "767,689 records of data availability per station and element"
        loaded_at_field: _LOADED_AT
        columns:
          - name: STATION_ID
          - name: LATITUDE
          - name: LONGITUDE
          - name: ELEMENT
          - name: FIRST_YEAR
          - name: LAST_YEAR

      - name: RAW_DAILY_OBSERVATIONS
        description: "Daily climate observations (TMAX, TMIN, PRCP, SNOW, etc.) partitioned by year"
        loaded_at_field: _LOADED_AT
        columns:
          - name: ID
            description: "Station ID"
          - name: DATE
            description: "Observation date in YYYYMMDD format"
          - name: ELEMENT
            description: "Observation type (TMAX, TMIN, PRCP, SNOW, SNWD, AWND, etc.)"
          - name: DATA_VALUE
            description: "Observation value — units vary by element"
          - name: M_FLAG
            description: "Measurement flag"
          - name: Q_FLAG
            description: "Quality flag"
          - name: S_FLAG
            description: "Source flag"
          - name: OBS_TIME
            description: "Observation time (HHMM)"
          - name: YEAR
            description: "Partition year derived from file path"
```

- [ ] **Step 2: Verify the file is valid YAML**

```bash
cd skyra-atmos/dbt/weather_analytics
python3 -c "import yaml; yaml.safe_load(open('models/staging/sources.yml'))" && echo "VALID"
```

Expected: `VALID`

- [ ] **Step 3: Commit**

```bash
cd skyra-atmos
git add dbt/weather_analytics/models/staging/sources.yml
git commit -m "feat: declare RAW source tables in dbt sources.yml"
```

---

### Task 5: Add README

**Files:**
- Create: `skyra-atmos/dbt/weather_analytics/README.md`

- [ ] **Step 1: Write README**

Create `skyra-atmos/dbt/weather_analytics/README.md`:

```markdown
# weather_analytics dbt Project

Transforms raw NOAA GHCN climate data from Snowflake RAW schema into
analytics-ready models in the STAGING and ANALYTICS schemas.

## Setup

1. Install dependencies: `dbt deps`
2. Ensure `~/.dbt/profiles.yml` has a `weather_analytics` profile (see design spec)
3. Load env vars: `source .env` from the repo root
4. Verify connection: `dbt debug`

## Schema Layout

| Schema     | Purpose                        | Materialization |
|------------|--------------------------------|-----------------|
| RAW        | Source data (managed by Airflow) | External        |
| STAGING    | Cleaned, typed, documented     | Views           |
| ANALYTICS  | Dimensional models, fact tables | Tables          |

## Commands

```bash
dbt deps              # install packages
dbt debug             # test Snowflake connection
dbt source freshness  # check RAW table recency
dbt compile           # validate all SQL compiles
dbt run               # run all models
dbt test              # run all tests
dbt docs generate && dbt docs serve  # browse documentation
```

## Sources

All source tables live in the `RAW` schema and are declared in
`models/staging/sources.yml`. They are loaded by the Airflow DAG
`ghcn_daily_incremental`.
```

- [ ] **Step 2: Commit**

```bash
cd skyra-atmos
git add dbt/weather_analytics/README.md
git commit -m "docs: add dbt project README"
```

---

### Task 6: Verify the complete setup

- [ ] **Step 1: Load env vars**

```bash
cd skyra-atmos
set -a && source .env && set +a
```

- [ ] **Step 2: Run `dbt debug`**

```bash
cd skyra-atmos/dbt/weather_analytics
dbt debug
```

Expected output (all green):
```
Connection:
  account: <your-account>
  user: <your-user>
  database: WEATHER_ANALYTICS
  warehouse: SKYRA_ATMOS
  role: <your-role>
  schema: STAGING
  ...
Connection test: [OK connection ok]
All checks passed!
```

If you see `Connection test: [ERROR]`, double-check:
- `.env` has real (non-placeholder) values
- `SNOWFLAKE_ACCOUNT` uses the format `<orgname>-<accountname>` (not the full URL)
- Your Snowflake user has access to the `WEATHER_ANALYTICS` database

- [ ] **Step 3: Run `dbt source freshness`**

```bash
dbt source freshness
```

Expected: All 5 sources listed. They may show as `error` or `warn` if no freshness threshold is set — that's fine at this stage. What matters is that dbt can **see** all 5 tables without errors like `relation not found`.

If you see `relation "WEATHER_ANALYTICS"."RAW"."RAW_STATIONS" does not exist`, the RAW tables haven't been loaded yet — run the Snowflake load scripts first (`snowflake/03_raw_tables.sql` and `snowflake/04_load_metadata.sql`).

- [ ] **Step 4: Run `dbt compile`**

```bash
dbt compile
```

Expected:
```
Found 0 models, 0 tests, 0 snapshots, 0 analyses, X macros, 0 operations, 0 seed files, 5 sources, 0 exposures, 0 metrics
Done.
```

0 models is correct — no models exist yet. 5 sources is the key number.

- [ ] **Step 5: Final commit**

```bash
cd skyra-atmos
git add dbt/
git commit -m "feat: complete dbt scaffold — debug, source freshness, compile all passing"
```

---

## Verification Checklist

Before marking Step 1 complete:

- [ ] `dbt debug` shows `All checks passed!`
- [ ] `dbt source freshness` lists all 5 RAW tables
- [ ] `dbt compile` shows `5 sources` with 0 errors
- [ ] `dbt_packages/` is present locally but not committed (gitignored)
- [ ] `~/.dbt/profiles.yml` is NOT committed to the repo
